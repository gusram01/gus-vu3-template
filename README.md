# @gusram01 Vue 3 template

- vite
- eslint
- jest
- postcss (preset env, nested, autoprefixer)

### run commands

```json
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "serve": "vite preview",
    "test:unit": "jest"
```
